namespace Entreprenly.WebServices.Iam.Domain.Model.Commands;

/// <summary>
///     Command to seed the catalog of system roles if they are not already present.
/// </summary>
public record SeedRolesCommand;
